# Bun Remix File Sharing demo
# Copy to .env and adjust as needed

FS_ADMIN_USER=admin
FS_ADMIN_PASSWORD=change-me

# Paths may be relative to the project root
FS_STORAGE_ROOT=./tmp/uploads
FS_DB_DIR=./tmp/db

# Optional: set your own secret (random 32+ chars). When empty, a random value is generated at runtime.
# FS_SESSION_SECRET=use-a-long-random-string

# Optional session lifetime in seconds (default 8 hours)
# FS_SESSION_MAX_AGE=28800

# Server port (optional, defaults to 44100)
# PORT=44100
